# Agent回答转文本文件 - 项目交接文档

> 本文档供其他智能体或开发者接手迭代时使用。请先完整阅读本文档，再查看 `相关文件/技术说明.md` 了解技术细节。

## 一、项目概述

### 是什么
一个网页工具，把 ChatGPT 等智能体的回答转化为 DOCX / PDF / HTML / Markdown 文件，**保留原始结构，只增强形式**（契合金字塔原理：结论先行、重点突出），生成便于阅读的文件。

### 给谁用
用户本人（研究者），日常会把 ChatGPT 的回答保存下来阅读和归档，需要一个工具让这些回答更易读、可导出为多种格式。

### 核心设计原则
1. **保留原始结构**：不重组原文的逻辑顺序、分段、箭头图，只对"形式"做增强
2. **重点突出**：结论句加粗+绿色背景，重点概念橙色高亮，关键词黄色标记
3. **单文件即用**：双击 `agent回答转文本文件.html` 即可在浏览器使用，无需安装环境
4. **多格式导出**：一键导出 DOCX / PDF / HTML / Markdown
5. **四种模板风格**：学术阅读、简洁、普通文档风、易读重排风，适应不同阅读场景

## 二、当前状态

### 完成度：100%（7/7 步骤全部完成 + 模板扩展迭代完成）

| 步骤 | 状态 |
|------|------|
| 1. 项目骨架 + 管理文档 | ✅ |
| 2. 输入区（粘贴+上传MD） | ✅ |
| 3. 结构识别器+强调标注器 | ✅ |
| 4. 模板渲染器+实时预览 | ✅ |
| 5. DOCX 导出 | ✅ |
| 6. PDF/HTML/MD 导出 | ✅ |
| 7. FLAMES 样本测试+修bug | ✅ |

### 已验证
- 用 FLAMES 学习路线规划文本做回归测试通过
- 四种格式导出全部正常，零控制台错误
- 识别统计：31 内容块 / 15 标题 / 3 结论 / 2 箭头流
- 四套模板（学术阅读/简洁/普通文档风/易读重排风）切换正常，预览和导出一致

## 三、文件结构

```
agent回答转文本文件/
├── agent回答转文本文件.html      # 主工具（单文件，双击即用）← 核心交付物
├── index.html                  # GitHub Pages 入口（自动跳转）
├── README.md                   # 项目说明
├── .gitignore                  # Git 忽略规则
├── 交接文档.md              # 本文档（项目交接）
└── 相关文件/
    ├── 技术说明.md          # 技术栈/架构/配置项说明
    ├── PROGRESS.md          # 进度监督文档（7步全部完成）
    └── CHANGELOG.md         # 错误记录 + 修改记录（含5条错误+5条修改）
```

## 四、技术架构

**方案 A：单文件 HTML + CDN 库**

- 纯前端，无后端、无构建步骤
- CDN 依赖：marked.js / file-saver.js / html2pdf.js / docx.js
- 所有代码内联在 `agent回答转文本文件.html` 中（约 1260 行）
- 详细技术说明见 `相关文件/技术说明.md`

## 五、核心处理流程

```
原始文本
  ↓
结构识别器（parseContent）→ 保留原文组织形式，只标注类型
  ↓
强调标注器（highlightInline）→ 行内高亮关键词
  ↓
模板配置（getTemplateConfig）→ 根据选中模板返回颜色/字体/背景配置
  ↓
模板渲染器（renderBlocks）→ 套用对应模板样式（四选一）
  ↓
实时预览 + 四种格式导出（预览与导出样式一致）
```

## 六、智能识别规则（启发式）

| 结构类型 | 识别特征 | 处理方式 |
|---------|---------|---------|
| 标题 | `##`/第X阶段/Step X/Module X/短行(3-25字无标点) | H1/H2/H3 样式 |
| 结论句 | 含"核心问题/目标/总结/真正的问题"等 | 绿色背景+加粗 |
| 重点概念 | `>>`开头或含"核心/关键/最重要" | 橙色背景+加粗 |
| 箭头流 | 区域预扫描：连续短行中含↓→ | 浅蓝背景+居中+箭头高亮 |
| 列表 | `-` `*` `•` `数字.` | 标准 bullet/number |
| 代码块 | ``` 包裹 | 等宽字体+灰底 |
| 行内关键词 | 核心/关键/最重要/重点/注意/不要/错误/失败 | 黄色背景高亮（仅学术模板） |

> **注意**：普通文档风和易读重排风不使用黄色关键词高亮，箭头流无背景色，色块无边框无圆角。

## 七、模板系统说明

项目支持四套模板，通过 `getTemplateConfig()` 函数统一管理配置。切换模板时，预览区和所有导出格式都会应用对应样式。

| 模板 | value | 风格特点 | 适用场景 |
|------|-------|---------|---------|
| 学术阅读模板 | `academic` | 深蓝/蓝/橙三级标题，绿色结论块，橙色重点块，黄色关键词高亮，圆角边框 | 通用场景 |
| 简洁模板 | `clean` | 去除装饰，纯净排版 | 极简偏好 |
| 普通文档风 | `plain` | 双色块（浅蓝软强调+浅橙硬强调），无边框无圆角，箭头流无背景 | 学术文档、正式报告 |
| 易读重排风 | `kami` | 普通文档风+Kami设计系统：暖羊皮纸底、墨蓝单色调、衬线标题、暖灰正文 | 长文阅读、沉浸式学习 |

### 普通文档风设计细节
- **软强调块**（结论/提示）：#F2F7FB 浅蓝底 + #1F4E79 深蓝斜体文字，无边框无圆角
- **硬强调块**（重点概念）：#FFF3E0 浅橙底 + #C55A11 橙色加粗文字 + `>>` 前缀，无边框无圆角
- **箭头流**：居中、加粗、深蓝文字，无背景色
- **标题**：统一深蓝色，通过字号大小区分级别
- **无黄色关键词高亮**

### 易读重排风设计细节（融合 Kami）
- 在普通文档风的双色块基础上：
- **页面底色**：#F5F4ED 暖羊皮纸色，非纯白
- **标题字体**：衬线体（思源宋体/STSong），字重 500
- **正文字体**：无衬线体，字重 400，暖灰色 #3D3D3A
- **行间距**：标题 1.3，正文 1.55（阅读优化）
- **代码块**：暖色边框 #E8E6DC，暖色底 #EFEEE6
- **设计理念**：Kami 极简——单一墨蓝强调色，暖色系，衬线标题提升正式感

### 如何添加新模板
1. 在 `getTemplateConfig()` 函数中添加新的 `if (template === 'xxx')` 分支，返回配置对象
2. 在模板选择器 HTML 中添加新的 `<option>`
3. 在 CSS 中添加 `[data-template="xxx"]` 样式规则
4. 在 `getExportStyles()` 中添加对应的导出 CSS 分支

## 八、已知限制与待改进方向

### 当前限制
1. **依赖网络**：CDN 库需在线加载，离线时降级为纯文本模式
2. **识别为启发式**：基于规则而非 AI，复杂文本可能误判
3. **模板扩展中**：已有四套模板（学术阅读/简洁/普通文档风/易读重排风），但用户不可自定义
4. **无持久化**：不保存历史记录，刷新即清空

### 未来迭代方向（按优先级）
1. **离线化**：将 CDN 库下载到本地，实现完全离线使用
2. **更多模板**：增加"技术文档""读书笔记"等模板，或支持用户自定义模板配置
3. **自定义规则**：让用户自定义高亮关键词和识别规则
4. **批量处理**：支持一次粘贴多个回答，分别导出
5. **AI 识别**：用轻量模型替代启发式规则，提高准确率
6. **历史记录**：用 localStorage 保存最近处理的内容

## 九、开发中遇到的关键问题（详见 CHANGELOG.md）

### Bug #1：箭头流未正确分组
- **问题**：原文中"疾病问题 ↓ GWAS是什么？ ↓ ..."被拆成独立元素
- **原因**：原逻辑仅在遇到↓行时触发，无法纳入前导文本行
- **修复**：新增 `isInArrowFlowRegion()` 预扫描函数，区域式检测

### Bug #2：短行过度识别为标题
- **问题**：箭头流中的文本行被误判为标题
- **原因**：`isTitleLine()` 短行规则过宽泛
- **修复**：箭头流检测前置于标题检测 + 增加排除规则（问句、连词开头）

### 环境问题：Python 不可用
- **问题**：测试时 Python HTTP 服务器报 `No module named 'encodings'`
- **原因**：PYTHONHOME 被覆盖
- **修复**：改用 Node.js 起 HTTP 服务器

### Bug #3：Write/SearchReplace 工具路径权限问题
- **问题**：使用 SearchReplace 和 Write 工具修改 HTML 文件时报错 `PathScopeExceed`
- **原因**：工具对含中文和特殊目录结构的路径有作用域限制
- **修复**：使用 Node.js 脚本在临时目录中读取原文件、执行字符串替换、写回临时文件，再用 `cp` 命令复制回原位置

## 十、如何接手迭代

### 快速上手
1. 双击 `index.html` 打开工具，粘贴一段智能体回答试用
2. 阅读 `相关文件/技术说明.md` 了解技术栈和配置项
3. 阅读 `相关文件/CHANGELOG.md` 了解已解决的问题

### 修改代码
1. 用 VS Code / 任意编辑器打开 `agent回答转文本文件.html`
2. CSS 在 `<style>` 标签内（约第 20-180 行）
3. JavaScript 在 `<script>` 标签内（约第 190 行起）
4. 关键函数：
   - `getTemplateConfig()` - **模板配置中心**，返回当前模板的颜色/字体/背景/字重等配置对象
   - `parseContent()` - 结构识别主逻辑（所有模板共用）
   - `isInArrowFlowRegion()` - 箭头流区域检测
   - `isTitleLine()` - 标题识别
   - `isConclusionLine()` - 结论句识别
   - `highlightInline()` - 行内关键词高亮（根据模板配置决定是否启用）
   - `renderBlocks()` - 模板渲染（根据模板添加 `>>` 前缀等）
   - `renderPreview()` - 预览渲染（设置 data-template 属性切换 CSS）
   - `getExportStyles()` - 导出样式生成（根据模板返回不同 CSS）
   - `exportDocx()` / `exportPdf()` / `exportHtml()` / `exportMd()` - 四种导出（全部支持模板切换）
5. 修改后保存，刷新浏览器即可

### 测试方法
1. 用 Node.js 起本地服务器：`node -e "require('http').createServer(...).listen(8765)"`
2. 或直接双击 `index.html` 打开
3. 粘贴 FLAMES 样本（见 `相关文件/PROGRESS.md` 中的测试数据）
4. 检查识别统计和预览效果
5. 测试四种格式导出

### 更新文档
每次迭代后更新：
- `相关文件/PROGRESS.md` - 更新步骤状态
- `相关文件/CHANGELOG.md` - 追加错误/修改记录
- `交接文档.md` - 如有重大变更，更新本文档

## 十一、项目元信息

- **创建日期**：2026-07-21
- **创建工具**：TRAE SOLO CN
- **技术方案**：方案 A（单文件 HTML）
- **测试样本**：FLAMES 算法迁移与改进的学习路线规划
- **文件总数**：4 个（agent回答转文本文件.html + 3 个管理文档）
- **代码行数**：约 1260 行（agent回答转文本文件.html）
- **模板数量**：4 套（学术阅读/简洁/普通文档风/易读重排风）
- **最近迭代**：2026-07-22 新增「普通文档风」和「易读重排风」两个模板
